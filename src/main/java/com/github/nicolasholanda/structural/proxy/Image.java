package com.github.nicolasholanda.structural.proxy;

/**
 * The common interface for both the real object and the proxy.
 */
public interface Image {
    /**
     * Displays the image.
     */
    void display();
}